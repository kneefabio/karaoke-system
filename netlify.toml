[build]
  base = "frontend"
  command = "yarn install --frozen-lockfile && yarn build"
  publish = "build"
  ignore = "git diff --quiet HEAD^ HEAD ./"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20.18.2"
